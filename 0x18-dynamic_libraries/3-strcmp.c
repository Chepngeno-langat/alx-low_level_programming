#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first parameter
 * @s2: second parameter
 * Return: returns compared string
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
