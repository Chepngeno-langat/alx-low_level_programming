#include "main.h"

/**
 * _isdigit - checks for a single digit
 * @c: parameter
 * Return: 0 or 1 according to condition
 */
int _isdigit(int c)
{
if (c >= '1' && c <= '9')
return (1);
else
{
return(0);
}
}
