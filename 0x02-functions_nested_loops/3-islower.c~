#include "main.h"

/**
 * _islower -> checks if character is lowercase
 * int c: character that is checked
 * Return: Returns 0 or 1 depending on condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
